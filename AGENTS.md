# Agent notes for L10nSharp

## Versioning

Refer to the Semantic Versioning section in README.md. When squashing a branch, make sure the surviving commit retains the highest-impact `+semver` marker (`major` over `minor`).
